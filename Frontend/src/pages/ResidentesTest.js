import React from 'react';

const ResidentesTest = () => {
  console.log('🚀 Componente ResidentesTest se está renderizando...');
  
  return (
    <div style={{ padding: '20px', backgroundColor: 'yellow', border: '2px solid red' }}>
      <h1 style={{ color: 'red', fontSize: '24px' }}>RESIDENTES TEST - COMPONENTE FUNCIONANDO</h1>
      <p>Si ves este mensaje, el componente se está renderizando correctamente.</p>
    </div>
  );
};

export default ResidentesTest;
